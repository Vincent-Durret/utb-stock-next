import React from 'react';
import OpenModal from '../ui/Button/OpenModal';

const SectionAddSubProduct = () => {
  return (
    <section>
      <OpenModal />
    </section>
  );
};

export default SectionAddSubProduct;
